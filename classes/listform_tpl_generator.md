# Class: listform_tpl_generator

Generates HTML for list views in ISPConfig modules, based on list definitions.

## Methods

- **buildHTML($listDef, $module = '')**
  - Builds and returns the HTML for a list view, using the provided list definition and module context. Also manages language files for the list.
